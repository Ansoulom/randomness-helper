{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Windows launch",
            "program": "${workspaceFolder}/build/randomness_test.exe",
            "args": [],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
